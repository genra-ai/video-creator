---
name: Fix Video Intent
description: "Describe the issue to generate a fix."
agent_description: "Use this skill when the generated video output does not match the user's core intent (e.g., requested a chase scene but got a conversation). It re-analyzes the shot description against the project goal and triggers a re-generation with corrected instructions. Triggered by: 'fix video intent', 'wrong scene', 'doesn't match intent', 'fix output'."
---

# Fix Video Intent

Re-aligns the visual output by re-parsing the original intent against the project goal and forcing a re-generation of the mismatched shots.
